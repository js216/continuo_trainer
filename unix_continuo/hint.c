/* hint.c --- suggest exercises or hints based on recent failures
 *
 * Monitors rolling history of last N attempts per lesson/step. Provides
 * hints when the user repeatedly fails or overpractices. Output is text
 * hints for GUI consumption. Input is line by line.
 *
 * Example input:
 *   RECENT_ATTEMPTS LESSON 3: FAIL, FAIL, FAIL
 *
 * Example output:
 *   HINT: Try a different exercise
 *   HINT: Focus on steps 2-4 only
 */

int main(void)
{
   return 0;
}
