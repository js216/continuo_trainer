/* analyzer.c --- track frequency and performance across realizations
 *
 * Reads user attempts and realizer outputs. Computes statistics on which
 * realizations are played, frequency counts, and average points per
 * realization. Input is line by line. Output is textual stats for GUI or
 * logging.
 *
 * Example input:
 *   REALIZATION 12: C4 E4 G4
 *
 * Example output:
 *   REALIZATION_STATS C4 E4 G4: TIMES_PLAYED 3 AVG_POINTS 850
 */

int main(void)
{
	return 0;
}
